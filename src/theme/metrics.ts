/** Espaçamentos recorrentes no Figma (grid de 375 x 812). */
export const metrics = {
  screenPadding: 24,
  radius: 8,
  buttonHeight: 56,
  avatarSize: 48,
  guildIconWidth: 64,
  guildIconHeight: 68,
  sectionGap: 32,
  labelGap: 12,
} as const;
